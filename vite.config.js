// The canvas engine (resources/assets/js/clinical-canvas.js) is deliberately not a
// Vite entry: Filament publishes it via `php artisan filament:assets`.
export const paths = [
    __dirname + '/resources/assets/sass/app.scss',
    __dirname + '/resources/assets/js/app.js',
];
